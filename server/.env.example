# Port the API server runs on
PORT=5000

# Comma-separated list of origins allowed to call this API (your frontend URLs)
CLIENT_ORIGIN=http://localhost:5173

# Gmail SMTP credentials used to send contact-form emails.
# Leave blank to skip emailing — submissions will still be logged/stored locally.
EMAIL_USER=
EMAIL_PASS=
EMAIL_TO=
